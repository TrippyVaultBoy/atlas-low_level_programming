#include <stdio.h>

/**
 * main- Entry point
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		if (a == 'e')
		{

		}
		else if (a == 'q')
		{

		}
		else
		{
			putchar(a);
		}
	}
	return (0);
}
