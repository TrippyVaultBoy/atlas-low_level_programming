#include "main.h"

/**
 * print_rev- prints a string in reverse
 * @s: pointer to string
 *
 * Return: void
 */

void print_rev(char *s)
{
	int length;

	for (length = 0; s[len] != '\0'; len++)
	{
	}

	int rev;

	for (rev = len; rev >= 0; rev--)
	{
		_putchar(*s);
	}
	_putchar('\n');
}
