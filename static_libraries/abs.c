#include "main.h"

/**
 * _abs- computed the absolute value of an int
 * @n: number to be checked
 *
 * Return: the absolute value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
