#include "main.h"

/**
 * _atoi- converts string to an integer
 * @s: input string
 *
 * Return: converted integer
 */

int _atoi(char *s)
{	
	if (*s == '\0')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
